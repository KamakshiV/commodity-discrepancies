# Optional: call Render directly (requires CORS_ORIGINS on backend to include your Vercel URL).
# If unset, use vercel.json proxy — browser calls /api on the same origin.
# VITE_API_URL=https://commodity-discrepancies.onrender.com/api
